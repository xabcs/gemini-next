##### 前端启动：
npm run dev

##### 登录相关

路由前置守卫路由在每次路由跳转之前，检查用户是否已登录（通过 token 是否为空判断）。

如果用户未登录且目标路由不是登录页，则强制跳转到登录页

put请求参数格式：

    {
    "expr":
        {
            "text": "2"
        },
    "current": 1, 
    "pageSize": 10
    }

返回数据结构格式：
成功并提供弹出信息的返回：

    {
        "code": 1000,
        "msg": "项目已创建"
    }

登录接口成功返回接口的格式：

    {
        "code": 1000,
        "msg": "success",
        "data": {
            "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjozOTExNjM3OTkzMDM4ODQ4MDAsInVzZXJuYW1lIjoiNjY2IiwiaXNzIjoid2ViX2FwcCIsImV4cCI6MTc2ODI4NjQ2OX0.svAuf0OZ20_GMFiKy-TrLERIwLMvuPua6zyKEQ-UHX0",
            "user_id": "391163799303884800",
            "user_name": "666"
        }
    }

错误返回格式：

    {
        "code": 1004,
        "msg": "用户名或密码错误"
    }

后端

>如果返回的code=2000  分两种情况 一种是返回了msg信息，一种是返回了data信息, 前端在处理的时候，如果有text信息就会出现弹框